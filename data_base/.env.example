# Used by the Python code (evaluate/, populate_db.py) and the db healthcheck.
DB_USER=user
DB_PASSWORD=password
DB_NAME=db
DB_HOST=localhost
DB_PORT=8023

# Used by the Postgres image to initialize the database. Keep in sync with DB_* above.
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=db

PT_FILE_NAME=voxceleb_embeddings.pt
METADATA_CSV_NAME=vox1_meta.csv